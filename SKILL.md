---
name: white-label-launch
description: "白牌新品从 0 到 1 的完整策划流程：从企业资产盘点出发，找到已被市场验证的标杆品牌，拆解其底层打法框架（而非表面执行），复用框架重做自己的产品定义、人群心理图、内容表达顺序与包装/定价方案，全程证据驱动 + 来源标签，交付前经独立验证通道审计，最终输出完整策略报告（Markdown）。适用于：白牌/无老客基础的品牌做新品立项、从 0 起盘、学标杆打法做差异化产品、配方或产品配比论证、人群定位与内容策略，跨品类通用（食品/宠物/男性消费品/硬件/服务等）。触发词包括「白牌起盘」「从 0 做新品」「对标拆解」「复用 XX 的打法」「人群心理图」「内容表达顺序」「新品立项报告」「帮我做一个完整的产品策划」等。当用户提供了企业资料、现有产品信息，或明确要求学习某个标杆品牌做新品时，必须使用本技能。"
---

# 白牌新品从 0 到 1 策划

一套被实战验证的决策流程：**先盘点自己，再找标杆，拆框架不拆表面，用框架重做自己的方案，证据驱动 + 来源标签，独立验证后输出报告**。

适用所有消费品类目。方法论在 `references/`（跨品类通用），品类知识与实战案例在 `knowledge/`（按品类沉淀，新项目时新建对应文件）。食养/大健康完整示例见 `knowledge/cases/tcm-powder.md`。

## 核心原则（先于一切流程）

1. **拆框架，不拆表面**。标杆的价值在底层打法（入口逻辑、信任结构、价格阶梯、人群画像、内容顺序），不在具体文案和包装颜色。照搬表面 = 死路；复用框架 = 省试错成本。
2. **证据门槛 + 来源标签（硬性）**。每个关键判断必须贴来源标签，四选一：**[本人提供]**（用户/企业资料）、**[已验证-来源]**（附链接/报告/法规条文）、**[AI推测]**（模型推断，待验证）、**[AI结论]**（多源推理综合）。绝不用行业平均数冒充用户企业的真实数据；报告的可信度声明按标签分类列明全部关键结论。用户拿不到证据的方案，等于没做。
3. **门禁（按决策不可逆性设两个固定卡点）**。纠偏是这个流程最大的价值来源，但卡点要设在「错了就全盘重来」的不可逆决策上，而不是机械地每个阶段都停。**全程最多强制停 2 次**：
   - **卡点一：标杆锁定后**（阶段 1 末）——选错标杆 = 整套框架复用错对象，阶段 2/3/4 全部白做；
   - **卡点二：内容判断标准确认后**（阶段 3 写内容前）——标准定错 = 「对错尺子」错了，所有内容产出返工。
   其余环节（资产盘点、拆解表、心理图、报告撰写）错了都是局部可修，自主跑完即可，不打断用户。
   **例外**：用户明确说「这两个卡点也自己定」时才全放。全放跳过的是「暂停等待」，不是验证——卡点 0 的验证通道与 verification-report.md 产出在任何模式下都不可跳过。无论走哪条路，**绝不伪造「用户已确认」**；全放模式下两个卡点决策须在报告中标注「未经用户确认，风险最高」。
4. **先立判断标准，再产内容**。涉及内容/文案/视觉的工作，先和用户确立一条可执行的判断标准（见 `references/psychology-and-content.md`，标准必须能区分「承认困境」与「羞辱用户」两个层次），之后所有产出用这条标准自检。
5. **数字论证必须自我闭合（硬性）**。任何定量**主张**（「这个克重有效」「这个价格有毛利」）不允许只报一个数——必须给出可验证的推导路径，或明确标注「假设，待打样/数据验证」。有来源的**描述性数字**（如「标杆售价 39.9 元[已验证-来源]」）不在此列，贴标签即可。对标竞品的规格只是定位参考，不是达标证明；与品牌母价值观矛盾的分量决策，禁止只写「随行就市」。
6. **区分两个成熟度**：概念级方案（卖什么、给谁、为什么）≠ 生产规格（配方定稿、稳定性、合规备案、首单成本）。每个产品产出必须显式标注属于哪一层；概念层不得默示已通过测试。流程末端是**卡点 0：验证通道**（见阶段 3.5），交付报告前必须完成并产出 verification-report.md。
7. **每次用户纠偏，必须沉淀回流程**：把纠偏抽象成可执行的新检查项，同步补进 `references/self-audit-checklist.md`（检查项）与 `references/failure-modes.md`（模式）——这是本技能的主要学习机制。纠偏记录必须与真实会话一致，不得为凑结构编造教训。

## 续跑检查（先于阶段 0）

开始任何阶段前，检查工作目录是否存在本技能产出的 `PROGRESS.md`。存在则读取并续跑，告知用户「发现上次进度，已完成 {阶段}，从 {下一阶段} 继续」。

`PROGRESS.md` 由本技能在阶段 0 创建、每阶段末更新，内容：项目名、当前阶段、调研深度档位、两个卡点的确认结论、来源模式（实时检索/知识回退）、待用户提供清单。

**项目目录约定**：每个项目在工作目录下建独立项目文件夹，过程文件（PROGRESS.md、资产盘点、标杆过滤与锁定、拆解表、本地化方案）与交付物（报告、verification-report.md）同目录存放、按阶段序号命名（01-/02-/03-/04-…）。

## 五阶段流程

流程默认连续推进，只在两个不可逆决策点停下来等用户确认（见原则 3）：**标杆锁定后**、**内容判断标准确认后**。每个卡点先给推荐方案 + 一句「为什么停在这里」，再请用户拍板。报告初稿完成后、交付前，必须经过**卡点 0：验证通道**（见阶段 3.5）。执行各阶段时对照 `references/failure-modes.md` 预防已知翻车模式。

### 阶段 0：资产与约束盘点（不研究竞品，先看自己有什么）

先做 **Question vs Problem 检测**：用户说的是「做什么」（question，如「帮我做一款助眠粉」）还是「解决什么」（problem）。把用户诉求复述为「谁 + 在什么场景 + 为什么买单」——复述不出来就先问清楚再盘点，避免在错误的命题上跑完整个流程。

然后收集并核实用户的真实家底，一项都不许用假设代替，每项贴来源标签：

- 企业资质与产线（认证、车间等级、产能、可做的剂型/形态）
- 现有产品与在售渠道、毛利要求
- 研发与内容资产（专家团队、配方储备、品牌资料）
- 约束条件：有没有老客、预算模式（如千川单品逻辑）、不做的事

产出：《资产盘点表》（每项带来源标签）+ 一页「我们能做什么、不能做什么」+ 创建 `PROGRESS.md`。
**常见错误**：跳过本步直接套行业假设 → 方案悬浮。必须先读用户上传的企业资料；资料缺失就列清单找用户要。企业自认的优势至少核验一条（实战教训：宣传的「特膳」实为运动营养标准，与目标人群不匹配）。

### 阶段 1：找标杆（找「已验证 + 可复制」的对标，不是找灵感）

先定**调研深度档位**（自主评估推荐，不新增卡点，记入 PROGRESS.md，用户可在卡点一推翻）：

| 档位 | 适用 | 调研量 |
|---|---|---|
| 轻 | 品类成熟、标杆明确、用户只要方向 | 每维度 1 轮检索 |
| 标准（默认） | 多数白牌新品 | 每维度 2–3 轮检索 + 关键数据双源交叉 |
| 深 | 新品类/高客单/高不可逆决策 | 每维度 3+ 轮 + 多源交叉 + 评论区语料采集 |

然后用**五重过滤法**筛标杆（排除「想当然」：第一反应想到的标杆必须过同样的过滤，不因为「大家都知道它」而直接入选；被过滤掉就如实记录）：

1. **赛道匹配**：同一品类或同一人群需求；
2. **价格带匹配**：落在用户企业做得起的价格带；
3. **人群匹配**：目标人群高度重合；
4. **增长可验证**：GMV、复购率、人群画像等有可查数据；
5. **资源可接触**：打法不依赖我们没有的资源（如标品心智、巨额代言、线下渠道）。

找不到达标的标杆时，如实告诉用户「这个方向没有被验证过的打法」，给出替代方向，而不是硬造一个对标。

**数据获取降级阶梯**（数据查不到时按此降级，每一步允许降级，不允许编造）：
1. 付费数据平台（蝉妈妈/飞瓜等，若环境可访问且有权限）
2. 品牌官方披露、权威媒体报道、招股书/财报
3. 电商页面可见的销量、评价数、价格带（自己抓取统计）
4. 以上皆不可得 → 在报告中明确标注「关键数据不可得」，并向用户列出建议采购的数据服务清单

产出：1 个主标杆 + 至多 2 个辅助参考，附数据来源与过滤过程记录。

**▶ 卡点一（必停）：标杆锁定。** 向用户呈现主标杆 + 五重过滤的逐项结果 + 数据来源 + 「我们能复用什么、不能复用什么」，给出推荐，请用户确认后再进入拆解。选错标杆 = 全链路返工，这是全程最值得停下来的一次。

### 阶段 2：拆解标杆（本技能最关键的一步）

按 `references/teardown-framework.md` 的清单逐项拆解，强制区分两层：

- **表层执行**（文案原文、包装色、达人名单）——标注「不可照搬」；
- **底层框架**（情绪入口逻辑、信任结构、价格阶梯、人群画像、内容顺序、复购设计）——标注「可复用资产」。

产出：《标杆拆解表》——左列表层执行（不可照搬），右列底层框架（可复用），每条框架附证据与来源标签。

### 阶段 3：框架复用 + 本地化重做

用标杆的框架，装用户自己的产品。产出四件套，每件都必须带「为什么」：

1. **产品定义**：配方/配比/含量/形态，逐项论证（成分的作用、品种与产地、法规身份、成本约束）。品类知识查 `knowledge/verticals/` 下对应文件（食品为 `food-cn.md`；其他品类没有就按同样结构新建），先排雷再定方。多 SKU 时先写一句**品牌母价值观**，每个 SKU 的克重/成分差异都要能用它推导；从单份成本反推克重，任何「坚持足量」的定量主张必须有量化论证，否则标「假设」。产品定义必须分两层输出——**概念级方案**（卖什么/给谁/为什么）与**落地生产规格**（配方定稿/稳定性/合规备案/成本）；未做打样与稳定性验证的，在报告中明确标注「概念级，待工程验证」。
2. **人群分层与心理图**：按 `references/psychology-and-content.md` 的五层心理图模板（表层行为→情绪真相→理想自我→决策链→禁忌，**每层必须标证据来源**），一个人群一张图。成分/特性不适合所有人时，分层是硬约束；购买者与使用者分离的品类（如宠物用品）做双层人群图。
3. **内容策略**：情绪入口用「TA 想成为的样子」；表达顺序按五位阶梯（理想画面→TA 想成为的状态→我为你改了什么→事实信任状→低门槛行动），产品/配方信息最早出现在第 3 位。内容判断标准分两层（见 `references/psychology-and-content.md`）：(a) 承认可控困境+给理想——可以写；(b) 用健康/外貌/人格贬低制造焦虑——禁止写（合规与尊重双红线）。

   **▶ 卡点二（必停）：内容判断标准确认。** 在写任何示范文案之前，先与用户确立一条可执行的判断标准（用正误对照表做底，附**本品类** 2–3 组「承认处境 vs 羞辱人格」对照示例——新品类须重新调研语境词，不得沿用其他品类的示例），给出推荐版本，请用户确认。标准定错 = 所有内容产出返工。确认后才动笔。
4. **包装与定价**：先做形态调研（袋/罐/盒/条装的市场验证情况）再下结论；定价沿用标杆验证过的阶梯结构作为**测试起点**，但必须从自己的单位经济模型（履约/扣点/佣金/CAC/退款）倒推验证毛利是否成立。

### 阶段 3.5（卡点 0，必做）：验证通道

报告初稿完成后、交付前，**必须**按 `references/verification-protocol.md` 执行验证：

1. 切换为对立评审视角，只以写出的文件为唯一事实源；
2. 跑 7 项通用检查（无标签论断/跨文件矛盾/数字闭合/数据缺口声明/概念级标注/陈旧数据/伪交叉验证）；
3. 按 `references/self-audit-checklist.md` 逐项打勾；
4. 产出 `verification-report.md`（Critical / Warning / Info 三级）作为正式交付物之一。

Critical 的处理不新增门禁卡点：事实/逻辑错误自主修复并记录闭环；必须用户输入才能修的列入「待用户提供」清单随交付呈现。任何模式下（含全放）验证通道与验证报告不可省略。

### 阶段 4：证据化输出完整报告

默认输出 **Markdown** 完整报告，保存到用户输出目录（Kimi 环境为 `/mnt/agents/output/`，其他环境用对应工作目录）。

**报告开头（第 1 章）必须是「可信度声明」**——按来源标签把全部关键结论分四类逐条列明：[本人提供] / [已验证-来源] / [AI推测] / [AI结论]，并附「关键数据不可得」清单。

**写作纪律（5 条）**：
1. 每个论断可溯源，标签跟随论断走，不在文末打包交代；
2. 禁止无证据形容词（「巨大」「领先」「深受喜爱」无来源即删）；
3. 数字必须闭环（原则 5 在报告层面的执行：主张要有推导，描述性数字要有来源）；
4. 营销套话禁用（「赋能」「抓手」「YYDS」类）——报告是决策文件，不是广告；
5. **推导单一事实源**：每个关键数字推导只在报告正文出现一次；过程文件（阶段产出稿）用引用（「见报告 4.2」），不复制数字——复制必然漂移（宠物食品验证实战教训：同一毛利推导两处各写一遍，漏算佣金未被发现）。

**通用骨架**（所有品类适用）：

1. 可信度声明（第 1 章，来源标签四类分列）
2. 纠偏判断标准（团队可执行的自检工具）
3. 产品方案总览（一张总表）
4. 每个产品一章：产品明细表（按下方品类适配）→ 设计逻辑 → 人群心理图（带证据来源） → 内容展现脚本（含表达顺序）→ 包装/形态方案
5. 内容表达顺序总纲 + 正误对照表
6. 包装形态/定价决策与视觉方向
7. 合规红线速查（如适用）
8. 参考文献（中文报告用 GB/T 7714，英文报告用 APA；正文上标一一对应；不得编造来源）

**品类适配表**（「产品明细表」按品类映射，不要硬套食品结构）：

| 品类 | 产品明细表的核心列 |
|---|---|
| 食品/食养 | 成分 · 品种/产地 · 法规身份 · 在配方中的角色 · 为什么 |
| 宠物食品 | 主原料/肉源 · 营养指标（蛋白/脂肪/钙磷比） · 法规身份（饲料标签） · 配方角色 · 为什么 |
| 硬件/家电 | 核心规格 · 关键物料/BOM · 认证 · 功能角色 · 为什么 |
| 服饰/美妆 | 面料/成分 · 版型/质地 · 安全认证 · 穿着/使用体验角色 · 为什么 |
| 服务/课程 | 模块 · 交付物 · 师资/资质 · 用户价值 · 为什么 |

共同列是「**每一项的为什么**」——明细表的具体列随品类变，「论证每一项为何存在」不变。

用户另行要求 PDF/docx 时，先出 Markdown 再按对应格式技能转换。

**交付前最终确认**：验证通道已通过（verification-report.md 已产出、Critical 已闭环或列入待用户提供）、报告结构与品类适配正确（没有把食品模板硬套到非食品品类）。

## 文件索引

方法论（references/，跨品类通用）：

| 文件 | 何时读 |
|------|--------|
| `references/teardown-framework.md` | 阶段 2 拆标杆时必读 |
| `references/psychology-and-content.md` | 阶段 3 做人群心理图与内容策略时必读 |
| `references/failure-modes.md` | 各阶段执行中对照预防；纠偏沉淀时更新 |
| `references/verification-protocol.md` | 阶段 3.5 验证通道必读 |
| `references/self-audit-checklist.md` | 阶段 3.5 验证通道逐项打勾；纠偏沉淀时更新 |

知识库（knowledge/，按品类沉淀；新品类立项时按既有文件结构新建）：

| 文件 | 何时读 |
|------|--------|
| `knowledge/verticals/food-cn.md` | 涉及中国食品/食养/大健康品类时必读 |
| `knowledge/cases/tcm-powder.md` | 需要完整示例（食养粉实战案例）时读 |

## 何时不要用本技能

本技能是「从 0 到 1 做新品完整策划」的重型流程。以下轻量需求直接用通用能力回答，不启动五阶段：

- 单次咨询（「帮我查一下某个竞品的价格」「这个成分能不能用」）
- 已有方案的局部润色、改文案、补一段内容
- 纯数据查询、单次合规查询

判断标准：用户要的是「一个答案」→ 直接答；要的是「一套从盘点到报告的完整立项方案」→ 启动本技能。

## 与其他技能的分工

若 deep-research 等研究类技能同时被触发：**本技能为主流程**（五阶段顺序、报告结构、参考文献格式以本技能为准）；deep-research 的检索纪律（多轮迭代搜索、来源核验）可在各阶段调研时借用，但其报告格式规则（如「不列参考文献」）不适用于本技能——本技能要求完整的参考文献列表。

## 红线

- 不编造数据、来源、案例；查不到就说查不到，并按阶段 1 的降级阶梯处理。
- 不用「行业报告假设」冒充用户企业的真实情况。
- 定量主张不允许「无数字定论」：凡主张克重、剂量、毛利、有效率，必须给出可验证的推算路径，否则明确标「假设」。
- 概念稿不得冒充生产规格；未打样/未验证的部分，交付时必须显式标注「概念级，待验证」。
- 内容红线两条。其一：不用健康/外貌/人格羞辱制造焦虑（疾病暗示、身材贬低等——合规与尊重双红线）。其二：可以承认可控的现实困境（太忙、没时间照顾自己），表达重心放在「TA 想成为的样子 + 我们如何帮 TA 做到」。
- 涉及食品/大健康：对外内容不出现疾病、症状、功效词（详见 `knowledge/verticals/food-cn.md`）；体质/禁忌分层只用于内部客服，不上包装、不上详情页。
- 两个固定卡点（标杆锁定、内容判断标准）必须停下来等用户确认；只有用户明确授权「自己定」才可跳过，且要在报告中标注「未经用户确认，风险最高」。
- 卡点 0（验证通道）在任何模式下不可跳过：必须执行验证并产出 verification-report.md。
- 每次用户纠偏后，把教训抽象成新检查项，同步追加到 `references/self-audit-checklist.md` 与 `references/failure-modes.md`；纠偏记录必须与真实会话一致。
